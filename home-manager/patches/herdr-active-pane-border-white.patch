diff --git a/src/ui/panes.rs b/src/ui/panes.rs
index b904ce00..86ab6927 100644
--- a/src/ui/panes.rs
+++ b/src/ui/panes.rs
@@ -479,9 +479,9 @@ fn render_pane_borders(
         let cell = &mut buf[(x, y)];
         cell.set_symbol(symbol);
         let color = if focused {
-            app.palette.accent
-        } else {
             app.palette.overlay0
+        } else {
+            app.palette.accent
         };
         cell.set_style(Style::default().fg(color));
     }
@@ -646,9 +646,9 @@ fn render_pane_border_titles(
             continue;
         }
         let color = if info.is_focused {
-            app.palette.accent
-        } else {
             app.palette.overlay0
+        } else {
+            app.palette.accent
         };
         let mut style = Style::default().fg(color);
         if info.is_focused {
@@ -1206,9 +1206,9 @@ mod tests {
 
         let buffer = terminal.backend().buffer();
         assert_eq!(buffer[(2, 2)].symbol(), "┼");
-        assert_eq!(buffer[(2, 2)].style().fg, Some(app.palette.accent));
+        assert_eq!(buffer[(2, 2)].style().fg, Some(app.palette.overlay0));
         assert_eq!(buffer[(2, 1)].symbol(), "│");
-        assert_eq!(buffer[(2, 1)].style().fg, Some(app.palette.accent));
+        assert_eq!(buffer[(2, 1)].style().fg, Some(app.palette.overlay0));
     }
 
     #[test]
@@ -1244,8 +1244,8 @@ mod tests {
             .unwrap();
 
         let buffer = terminal.backend().buffer();
-        assert_eq!(buffer[(1, 1)].style().fg, Some(app.palette.accent));
-        assert_eq!(buffer[(2, 1)].style().fg, Some(app.palette.overlay0));
+        assert_eq!(buffer[(1, 1)].style().fg, Some(app.palette.overlay0));
+        assert_eq!(buffer[(2, 1)].style().fg, Some(app.palette.accent));
     }
 
     #[tokio::test]
