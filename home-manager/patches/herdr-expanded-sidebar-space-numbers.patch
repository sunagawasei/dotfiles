diff --git a/src/ui.rs b/src/ui.rs
index a4e4728..b9f4e64 100644
--- a/src/ui.rs
+++ b/src/ui.rs
@@ -990,7 +990,7 @@ mod tests {
     }
 
     #[test]
-    fn expanded_sidebar_workspace_rows_show_state_before_name_without_numbers() {
+    fn expanded_sidebar_workspace_rows_show_state_before_name_with_numbers() {
         let mut app = crate::app::state::AppState::test_new();
         let mut ws = Workspace::test_new("one");
         let repo = temp_git_repo("main");
@@ -1018,9 +1018,8 @@ mod tests {
         let line1 = buffer_row_text(buffer, card, card.y);
         let line2 = buffer_row_text(buffer, card, card.y + 1);
 
-        assert!(line1.starts_with(" · one"));
-        assert!(!line1.contains("1 one"));
-        assert_eq!(line2, "   main");
+        assert!(line1.starts_with("1 · one"));
+        assert_eq!(line2, "    main");
 
         std::fs::remove_dir_all(repo).ok();
     }
diff --git a/src/ui/sidebar.rs b/src/ui/sidebar.rs
index ebe7d0a..cada63f 100644
--- a/src/ui/sidebar.rs
+++ b/src/ui/sidebar.rs
@@ -1162,6 +1162,15 @@ fn render_workspace_list(
         } else {
             p.overlay0
         });
+        let number = format!("{} ", i + 1);
+        let number_width = number.len();
+        let number_style = if selected {
+            Style::default().fg(p.overlay1)
+        } else if is_active {
+            Style::default().fg(p.text)
+        } else {
+            Style::default().fg(p.overlay0)
+        };
         let token_values = ws.metadata_tokens.values();
         let rows = tokens::space_rows(
             &app.sidebar_spaces,
@@ -1181,6 +1190,7 @@ fn render_workspace_list(
             }
             let mut spans = Vec::new();
             if row_index == 0 {
+                spans.push(Span::styled(number.clone(), number_style));
                 if card.indented {
                     spans.push(Span::raw("   "));
                 } else if let Some((_, collapsed)) = parent_group.as_ref() {
@@ -1189,25 +1199,33 @@ fn render_workspace_list(
                         Style::default().fg(p.accent),
                     ));
                     spans.push(Span::raw(" "));
-                } else {
-                    spans.push(Span::raw(" "));
                 }
             } else {
-                spans.push(Span::raw(if card.indented { "     " } else { "   " }));
-            }
-            let prefix_width = if row_index == 0 {
-                if card.indented {
-                    3
+                let prefix_width = if card.indented {
+                    5
                 } else if parent_group.is_some() {
+                    4
+                } else {
                     2
+                };
+                spans.push(Span::raw(" ".repeat(number_width + prefix_width)));
+            }
+            let content_prefix_width = number_width
+                + if row_index == 0 {
+                    if card.indented {
+                        3
+                    } else if parent_group.is_some() {
+                        2
+                    } else {
+                        0
+                    }
+                } else if card.indented {
+                    5
+                } else if parent_group.is_some() {
+                    4
                 } else {
-                    1
-                }
-            } else if card.indented {
-                5
-            } else {
-                3
-            };
+                    2
+                };
             spans.extend(resolved_token_spans(
                 resolved,
                 state_icon,
@@ -1216,7 +1234,7 @@ fn render_workspace_list(
                 branch_style,
                 branch_style,
                 p,
-                card.rect.width.saturating_sub(prefix_width) as usize,
+                (card.rect.width as usize).saturating_sub(content_prefix_width),
             ));
             frame.render_widget(
                 Paragraph::new(Line::from(spans)),
