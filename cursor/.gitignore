# Cursor CLI - Git除外設定

# チャット履歴（ローカルのみ保持）
chats/

# バックアップファイル
*.backup
*.bak
*.tmp

# システムファイル
.DS_Store
Thumbs.db

# 機密情報を含む設定（必要に応じて調整）
# cli-config.jsonにはauthInfo（認証情報）が含まれているため、
# リポジトリに含めるかどうかは慎重に判断してください。
# 個人利用の場合は問題ありませんが、チームで共有する場合は
# 機密情報を除外したテンプレートファイルを作成することを推奨します。

# 例: チームで共有する場合は以下を有効化
# cli-config.json
